# Brand assets

Drop the final brand asset bundle here when brand delivers it:

- `logo.svg` (full lockup, MIF Blue on transparent)
- `logo-on-dark.svg` (white on transparent, for the sidebar on dark surfaces)
- `mark.svg` (just the `N` glyph)
- `favicon.ico`, `apple-touch-icon.png`, `icon.svg`
- `og-image.png` (1200×630, used by `metadata.openGraph.images`)

Until the bundle lands, the lockup is rendered as a CSS-only `N` glyph in the primary-coloured square — see `src/components/app-shell/sidebar.tsx`.
